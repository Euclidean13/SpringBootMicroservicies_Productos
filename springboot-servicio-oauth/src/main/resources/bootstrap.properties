# In Bootstrap must be configured everything related with Config Server
spring.application.name=servicio-oauth
spring.cloud.config.uri=http://localhost:8888

# To include all the actuator endpoints to enable the configuration auto-refresh
management.endpoints.web.exposure.include=*